**字节序**

 - QImage 默认字节序 B0 G1 R2 A3
 - OpenCV 默认字节序 B0 G1 R2 
 - FreeImage 默认也是 B0 G1 R2 A3
以上默认Little Endian

-----------

**矩阵** resize / reshape

 - numpy、opencv是行序列化，即从左到右，从上到下 
 - eigen、matlab是列序列化，即从上到下，从左到右
